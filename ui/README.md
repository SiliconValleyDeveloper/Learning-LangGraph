# LangGraph Lab UI

Angular chat UI for the LangGraph Lab ReAct agent.

## Development server

```bash
npm start
```

Navigate to `http://localhost:4200/`. The app reloads when you change source files.

## Build

```bash
npm run build
```

Build artifacts are written to `dist/langgraph-lab-ui`.
